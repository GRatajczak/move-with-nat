---
import AuthenticatedLayout from "../../../layouts/AuthenticatedLayout.astro";
import { PlansListPage } from "../../../components/plans/view/PlansListPage";

const { locals } = Astro;
const userId = locals.user?.id ?? "";
---

<AuthenticatedLayout requiredRole="trainer">
  <PlansListPage client:load userRole="trainer" userId={userId} />
</AuthenticatedLayout>
